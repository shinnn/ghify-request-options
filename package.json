{
  "name": "ghify-request-options",
  "version": "1.0.0",
  "description": "Create a Request option object that helps to interact with the GitHub API",
  "repository": "shinnn/ghify-request",
  "author": "Shinnosuke Watanabe (https://github.com/shinnn)",
  "scripts": {
    "pretest": "eslint --config @shinnn/node --rule 'complexity: [error, 15]' index.js test.js",
    "test": "node --harmony_destructuring --throw-deprecation --track-heap-objects test.js | tap-spec",
    "coverage": "node --harmony_destructuring node_modules/.bin/istanbul cover test.js"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "keywords": [
    "request",
    "http",
    "https",
    "github",
    "api",
    "github-api",
    "option",
    "options",
    "default",
    "defaults",
    "integration"
  ],
  "devDependencies": {
    "@shinnn/eslint-config-node": "^2.0.0",
    "eslint": "^2.7.0",
    "istanbul": "^0.4.2",
    "request": "^2.70.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1"
  }
}
